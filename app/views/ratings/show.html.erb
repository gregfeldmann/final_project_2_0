<div class="page-header">
  <h3>Rating #<%= @rating.id %></h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>Entertainment</dt>
      <dd>
        <% if @rating.entertainment.present? %>
          <a href="/entertainments/<%= @rating.entertainment_id %>">
            <%= @rating.entertainment.title %>
          </a>
        <% end %>
      </dd>

      <dt>Friend</dt>
      <dd>
        <% if @rating.friend.present? %>
          <a href="/friends/<%= @rating.friend_id %>">
            <%= @rating.friend.name %>
          </a>
        <% end %>
      </dd>

      <dt>Rating</dt>
      <dd><%= @rating.rating %></dd>

      <dt>Url</dt>
      <dd><%= @rating.url %></dd>

      <dt>Title</dt>
      <dd><%= @rating.title %></dd>

      <dt>Friend</dt>
      <dd><%= @rating.friend %></dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/ratings" class="btn btn-primary">
        Back
      </a>
      <a href="/ratings/<%= @rating.id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/delete_rating/<%= @rating.id %>" class="btn btn-danger" rel="nofollow">
        Delete
      </a>
    </div>
  </div>
</div>
